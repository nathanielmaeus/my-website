import React from 'react';
import PropTypes from 'prop-types';
import { Text } from './skills.css';

const Skills = () => {
  return (
    <Text>
      Things that I work with: React, Redux, Typescript, SCSS, CSS-in-JS,
      SvelteJS, GatsbyJS, NextJS
    </Text>
  );
};

export default Skills;
